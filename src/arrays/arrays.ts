export const map = <T, R>(array: T[], cb: (t: T) => R): R[] => {
  return [];
};

export const filter = <T>(array: T[], cb: (t: T) => boolean): T[] => {
  return [];
};

export const reduce = <T, R>(array: T[], cb: (a: R, t: T) => R, start: R): R => {
  return start;
}
